# My Project

## Overview
This project allows for customizable campaign management with user authentication for admins.

## Getting Started

### Prerequisites
- Node.js
- MongoDB

### Installation
1. Clone the repository.
2. Navigate to the backend folder.
3. Run `npm install` to install dependencies.
4. Start the server with `node server.js`.

### Frontend
1. Navigate to the frontend folder.
2. Run `npm install` and then `npm start` to launch the React app.

### API Endpoints
- `POST /api/register` - To register a new admin.
- `POST /api/login` - For admin login.
- `POST /api/sliders` - To configure sliders.
